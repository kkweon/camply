{
    "branches": [
        "develop",
        "main",
        "master",
        "next",
        "next-major",
        "+([0-9])?(.{+([0-9]),x}).x",
        {
            "name": "beta",
            "prerelease": true
        },
        {
            "name": "alpha",
            "prerelease": true
        }
    ],
    "plugins": [
        "semantic-release-gitmoji",
        [
            "@semantic-release/exec",
            {
                "prepareCmd": "uv version ${nextRelease.version} && uv build && task docs -- build",
                "publishCmd": "task publish RELEASE_VERSION=${nextRelease.version} CI=true"
            }
        ],
        [
            "@semantic-release/git",
            {
                "assets": ["pyproject.toml", "uv.lock"],
                "message": "🔖 camply ${nextRelease.version}\n\n${nextRelease.notes}\n[skip ci]"
            }
        ],
        [
            "@semantic-release/github",
            {
                "assets": [
                    {
                        "path": "dist/*.whl"
                    },
                    {
                        "path": "dist/*.tar.gz"
                    }
                ]
            }
        ]
    ]
}
